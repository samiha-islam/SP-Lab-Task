#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	int sum=0,lastdigit=0;
	while(n!=0)
    {
        lastdigit=n%10;
        sum+=lastdigit;
        n=n/10;
    }
    printf("Sum of digits:%d",sum);

    return 0;
}
